import React from 'react'

const Cardtemporadas = ({ id = {} }) => {
  return (
    {/* <section>
      <h2>Temporadas</h2>
      <div className=''>
        {
          loading
            ? <p className='text-center'> Cargando... </p>
            : pelis.map((peli, index) => (
              <aside key={index} className='col-md-3 col-lg-2'>
                <Cardtemporadas {...peli} />
              </aside>
            ))
    }
      </div>

    </section> */}
  )
}

export default Cardtemporadas
